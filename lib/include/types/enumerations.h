#pragma once

#include <string>
#include <unordered_map>

#include "typenames.h"
#include "constants.h"
#include "exceptions.h"


enum OutputFromat {
    console = 0,
    file = 1,
    disabled = 2,
};

